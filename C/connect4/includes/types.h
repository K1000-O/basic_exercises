#ifndef TYPES_H
#define TYPES_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <strings.h>
#include <strings.h>

typedef enum {
    ERROR = 0, 
    OK = 1
} status;

typedef enum {
    False = 0, 
    True = 1
} Bool;

#endif